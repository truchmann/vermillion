const cities = []
const names = ["Rome", "Amsterdam", "Lisbon", "Frankfurt", "Prague"]

for (let i = 0; i < names.length; i++) {  
    console.log(names[i]); 
    cities[i] = names[i];   
}

# vermillion
